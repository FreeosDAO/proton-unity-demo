using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class Text_TXID : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Text txt = GameObject.Find("Canvas/Text_TXID").GetComponent<Text>();
        //txt.text = "test change text";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
